import styled from "styled-components";
import { Link } from "react-router-dom";

export const routeTips = styled.div`
  display: flex;
  gap: 8px;
`;

export const Links = styled(Link)`
  text-decoration: none;
`;
